---
id: T-011-01
story: S-011
title: v100-qa-scroll-nav-overlay-fixes
type: task
status: open
priority: high
phase: implement
depends_on: []
---

## Context

QA pass on V100/V100.html caught broken scrolling, overlay containment issues, navigation dead-ends, and visual noise. All land in a single file edit.

## Scope

### P0 Fixes
1. Response screen scroll clearance — `padding-bottom` equal to bottom-nav height so action row never hides under nav
2. Overlays (`#overlay-preview`, `#overlay-loading`) — change from `position: fixed` to `position: absolute` to contain within `#app` shell
3. Lens screen back button — add a `← Back` btn-ghost that calls `goToLanding()`, styled per theme
4. "New" clears vent — `goToLanding()` should also clear `#ventText`, reset char counter

### P1 Fixes
5. Remove cyberpunk h1 gradient — replace `background: linear-gradient` clip with flat `color: var(--c1); -webkit-text-fill-color: var(--c1)`
6. Remove kawaii add-card gradient — replace with flat `var(--bg-card2)` or `#f5f0ff`
7. Fix initial `maxlength="800"` → `"450"` in HTML to match THEME_COPY
8. `quote-text` line-clamp — replace `max-height: 160px; overflow-y: auto` with `-webkit-line-clamp: 4` + `display: -webkit-box`

### P2 Fixes
9. "Continue" stub — replace `alert()` with a brief in-screen bottom toast/snackbar
10. `ventError` transition — add `max-height` + `opacity` CSS transition so it slides in
11. Bottom nav label trim — "Try another" (already short) — verify it fits on 320px; reduce if needed

## Acceptance Criteria
- Scrolling to bottom of response screen shows action row above nav, not hidden behind it
- On 1440px desktop, overlays stay within the 420px app shell
- Lens screen has a visible back button on all three themes
- "New" button clears the textarea content + char counter
- Cyberpunk h1 reads as flat neon cyan, no gradient clip
- No `alert()` calls in user flow
