---
id: T-017-02
story: S-017
title: onboarding-screen
type: task
status: open
priority: high
phase: ready
depends_on: []
---

## Context

Rebuild `/app/onboarding` to match the Figma "Landing Kawaii" design.

Figma node: `416:7498` — "Landing Kawaii"
File: `Mubv0Ghdm2SPxF42JVsX8M`

## What the screen looks like (from Figma)

Two sections stacked vertically, full pink background:

### Top section — vent input
1. **Header card** (white rounded card, same token system):
   - "GET IT OFF YOUR CHEST" — Bold 28px, `var(--cyan)` (pink in kawaii), uppercase, centered
   - Subtext — "Let it all out — then see it through the eyes of someone who lived through years of history." — Regular 14px, centered

2. **Vent textarea** (two-part component):
   - **Header row** — mint bg (`var(--input-header-bg)`, `var(--input-header-shadow)`), rounded top corners (32px), label "DUMP IT ALL HERE:" — Bold 12px, uppercase, centered
   - **Body** — white bg, rounded bottom corners, `<textarea>` full height, placeholder "Start typing...", Regular 14px body text
   - **Footer row** inside body — right-aligned character counter "X/800 CHARACTERS" — Regular 10px, `var(--text-sub)`
   - Max 800 characters enforced

3. **"SELECT THE LENS" button** — primary button style (`var(--btn-bg)` etc.), full width, navigates to `/app/lens`, disabled if vent is empty

### Bottom section — coming soon teaser
4. **"Mind-mapping tool" card** — same card style:
   - "COMING SOON" label — Bold 12px, `var(--text-body)`, uppercase
   - "MIND-MAPPING TOOL" — Bold 18px, `var(--cyan)`, uppercase
   - Description text — Regular 14px, `var(--text-body)`, centered
   - Category tags line — Regular 10px, uppercase, `var(--text-sub)`
5. Text: "Get on the waitlist to be the first to know"
6. **"SIGN UP" button** — secondary button style (`var(--btn-secondary-bg)`, `var(--btn-secondary-shadow)`)

## State management

- Vent text stored in `sessionStorage` key `ms_vent` (existing pattern)
- "SELECT THE LENS" button disabled when textarea is empty

## Acceptance Criteria

- Matches Figma screenshot visually in kawaii theme
- Textarea is functional (typing, char count updates live)
- Vent saved to sessionStorage on button click before navigation
- Uses only CSS custom properties
- Works across all 3 themes

## Files

- `V200/src/app/app/onboarding/page.tsx` — full replacement
