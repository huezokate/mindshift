---
id: T-017-01
story: S-017
title: welcome-screen
type: task
status: open
priority: high
phase: ready
depends_on: []
---

## Context

Rebuild `/` (the welcome landing page) to match the Figma design exactly.

Figma node: `414:7231` — "Welcome Landing - Selected at random"
File: `Mubv0Ghdm2SPxF42JVsX8M`

The current `/` has a basic layout. Replace it with the Figma design.

## What the screen looks like (from Figma)

- Full-screen pink background (`var(--bg)`)
- Centered white card (`var(--card-bg)`, `var(--card-radius)`, asymmetric border + inset shadow via CSS vars)
- Inside card:
  - "MINDSHIFT" heading — `var(--font-display)` Bold 28px, `var(--cyan)` color (maps to #ff50c5 in kawaii), uppercase, centered
  - Tagline — "You're not lost. Just looking at the map upside down." — Bold 12px, `var(--violet)` color, uppercase, centered, tracked
  - Body copy — 14px regular, `var(--text-body)`, centered
  - "ENTER THE SPACE" button — `var(--btn-bg)`, `var(--btn-bl/br/bt/bb)`, `var(--btn-radius)`, `var(--font-btn)`, `var(--btn-color)`, uppercase
- Below card (not inside):
  - "A FEW THINGS BEFORE WE BEGIN:" — Bold 12px, `var(--violet)`, uppercase, centered
  - Disclaimer body text — Regular 14px, `var(--text-body)`, centered
  - "NOTHING HERE IS CLINICAL ADVICE." — Bold 12px, `var(--violet)`, uppercase, centered

## Button behavior

"ENTER THE SPACE" → navigates to `/app/onboarding`

## Acceptance Criteria

- Matches Figma screenshot visually when `data-theme="kawaii"`
- Uses ONLY CSS custom properties, no hardcoded hex
- Works across cyberpunk and notepad themes (tokens apply automatically)
- Framer Motion fade-in on card and disclaimer (existing animation pattern)

## Files

- `V200/src/app/page.tsx` — full replacement
